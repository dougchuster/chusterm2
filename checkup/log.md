# Check-up de páginas — Log de correções

Formato: `[data] Rota | Problema | Causa raiz | Correção | Arquivos alterados | Resultado`

Evidências brutas: `checkup/probe/results/*.json` (varredura por persona/largura), `checkup/probe/results/auth.json` (auth funcional). Screenshots em `checkup/probe/shots/` (não versionados).

## A. Autenticação

- `[2026-09-18] /app/login (A1)` | Credencial inválida mostra mensagem em inglês ("Invalid login credentials. Please try again.") apesar de `default_locale = pt_BR` | O gem `devise_token_auth` só traz locale `pt-BR` (hífen); o app usa `pt_BR` (underscore) — a chave nunca casa e o I18n cai no fallback `en`. Nenhum arquivo do repo cobria `devise_token_auth.*` em pt_BR | Novo `config/locales/devise_token_auth.pt_BR.yml` (cópia do pt-BR do gem, chave renomeada). Cobre também mensagens de reset/confirmação/registro | `core/config/locales/devise_token_auth.pt_BR.yml`, `core/spec/controllers/devise_overrides/sessions_controller_spec.rb` (+1 exemplo) | RSpec 3/0. Runtime: aguarda rebuild da imagem `core` (container roda `RAILS_ENV=production` sem bind mount)
- `[2026-09-18] /app/login (A1)` | Demais itens do checklist | — | Sem correção: validação client (Vuelidate) + server (401), botão `:disabled="loginApi.showLoading"` (anti duplo-submit), Rack::Attack `login/ip` 5/5min confirmado (6ª tentativa → 429), 401 não vaza stack, logado em `/app/login` → redirect p/ dashboard, axe 0 serious/critical, sem overflow 360/768/1440, 0 erros de console | — | Evidência `probe/results/auth.json`
- `[2026-09-18] /app/login/sso (A2)`, `/app/auth/signup (A3)`, `/app/auth/confirmation (A4)` | — | — | A3: signup desabilitado → redireciona p/ login (config, não bug). A4 com token inválido: API 422 e volta p/ login sem mensagem — comportamento upstream; UX poderia avisar "link expirado" (registrado como recomendação, não alterado sem aprovação) | — | OK
- `[2026-09-18] /app/auth/password/edit (A5)` | Campos "Senha"/"Confirmar senha" sem `<label>` (só placeholder) e `<label for>` do `FormInput` não apontava para nenhum `id` | `v3/components/Form/Input.vue` não dava `id` ao `<input>`, então o `for=name` do `WithLabel` ficava órfão em **todas** as páginas v3 (signup, reset, edit) | `:id="name"` no input (antes de `v-bind="$attrs"`, então `id` explícito ainda vence) + `:label` nos dois campos de A5. Páginas impactadas pelo arquivo compartilhado: A3 signup, A5, A6 — re-testar as três | `core/app/javascript/v3/components/Form/Input.vue`, `.../auth/password/Edit.vue`, `core/app/javascript/v3/components/Form/Input.spec.js` (novo, 3 exemplos) | Vitest 3/0, ESLint limpo. Runtime aguarda rebuild
- `[2026-09-18] /app/auth/reset/password (A6)` | Link "clique aqui" (voltar ao login) aponta para `/auth/login` — rota inexistente no router v3; carregado direto no Rails devolve `307 → /omniauth/login?resource_class=User` | Caminho digitado sem o prefixo `/app` (todas as outras telas usam `/app/login`) | `to="/app/login"` | `core/app/javascript/v3/views/auth/reset/password/Index.vue` | Aguarda rebuild p/ clique real
- `[2026-09-18] /app/auth/reset/password (A6)` | Campo e-mail sem `<label>` | `FormInput` sem prop `label` | `:label="$t('RESET_PASSWORD.EMAIL.LABEL')"` (chave já existia) | idem | idem
- `[2026-09-18] /app/auth/reset/password (A6)` | axe `color-contrast` serious: link `text-n-brand` (#2781f6) 14px sobre branco = 3.77:1 (< 4.5 AA) | Token de cor do design system (`n-brand`) usado como cor de texto pequeno | **Não corrigido** — mudar o token afeta o sistema inteiro; decisão de design. Recomendação no relatório: `text-n-brand` só em ≥18px/bold ou criar `n-brand-text` mais escuro | — | BLOQUEADO (design)

## Infra do check-up

- `[2026-09-18] (todas as rotas)` | Container `core` local rodava imagem de 16/09 (`RAILS_ENV=production`, código embutido) — 15 commits e 5 migrations atrás do HEAD; `crm/packs` dava 404 e `crm_board_views.context`/`crm_deal_conversations`/`crm_account_packs` não existiam | Imagem não é rebuildada automaticamente | `docker compose build core sidekiq && up -d` + `db:migrate` + `qa:seed`. **Obs.:** o `docker-compose.override.yml` (ignorado pelo git) que mapeava a porta 8086 não existe mais — o `core` recriado subiu na porta canônica **3010**; QA passa a usar `QA_BASE_URL=http://127.0.0.1:3010` | — | Ambiente = HEAD `c038738a69` + correções deste check-up
- `[2026-09-18] (deploy) db/migrate/20260918000001_create_crm_deal_conversations.rb` | **CRÍTICO** — `db:migrate` aborta com `PG::ForeignKeyViolation` ao fazer backfill de `crm_deal_conversations` | `crm_deals.conversation_id` não tem FK; existem negócios apontando para conversas apagadas (2 no banco local, conta 1). O `INSERT … SELECT` copiava esses ids e violava a FK nova, cancelando a migration e as 3 seguintes | `AND EXISTS (SELECT 1 FROM conversations c WHERE c.id = d.conversation_id)` no backfill | `core/db/migrate/20260918000001_create_crm_deal_conversations.rb`, `core/spec/migrations/create_crm_deal_conversations_spec.rb` (novo) | RSpec 1/0; migration aplicada no banco local (8 elos criados, 2 órfãos pulados). **Antes de deployar nas VPS**, rodar `SELECT count(*) FROM crm_deals d WHERE conversation_id IS NOT NULL AND NOT EXISTS (SELECT 1 FROM conversations c WHERE c.id=d.conversation_id)` para dimensionar

## CRM — funcional (API ponta a ponta, `probe/crm-crud.mjs`, 61 checks)

- `[2026-09-18] …/crm (board), …/crm/leads` | **Arrastar um card para a coluna "Ganho"/"Perdido" deixava o negócio `status=open`** dentro da coluna terminal — fora de toda métrica de fechamento (`closed_at`), e "reabrir" não fazia nada | `Crm::DealMover` troca a etapa mas não olha `terminal_outcome`; só `mark_won!/mark_lost!` gravam status (e eles chamam o mover depois) | `sync_terminal_status!` no mover: entrar em coluna terminal fecha (won/lost + `closed_at` + audit); sair de terminal por arrasto reabre. No-op quando `mark_won!` já gravou | `core/app/services/crm/deal_mover.rb`, `core/spec/services/crm/deal_mover_spec.rb` (+4) | RSpec 83/0 (mover, crm_deal, tools, deals_controller). Runtime confirmado após rebuild: move → `status=won`
- `[2026-09-18] …/crm/settings/loss-reasons` | **Criar motivo de perda pela UI falhava** (422 `Slug can't be blank`) | `LossReasons.vue` envia só `name`; `CrmLossReason` exige `slug` e não o gera (CrmPipeline/Stage têm `ensure_slug`) | `before_validation :ensure_slug` | `core/app/models/crm_loss_reason.rb`, `core/spec/models/crm_loss_reason_spec.rb` (novo, 4) | Runtime: criar/editar OK
- `[2026-09-18] (todas as páginas com formulário)` | Erros de validação do servidor chegavam em inglês ("Validation failed: Title can't be blank") | `default_locale = pt_BR` mas sem traduções do framework (rails-i18n ausente) | `config/locales/rails.pt_BR.yml` (errors.messages, record_invalid, atributos comuns como fallback global) | `core/config/locales/rails.pt_BR.yml`, `core/spec/config/locales/pt_br_framework_translations_spec.rb` | Runtime: `"A validação falhou: Título não pode ficar em branco"`
- `[2026-09-18] …/crm/settings/cadences` | Inscrever negócio em cadência não checa consentimento do contato (D6 do PLANO_17_09) | Regra de negócio ainda não implementada | **Não alterado** (regra de negócio — decisão sua); registrado como ALTO no relatório | — | BLOQUEADO (decisão)
- `[2026-09-18] CRM — o resto` | — | — | Verificado OK: funil/etapa CRUD + arquivar/restaurar; negócio criar/editar/mover/won/lost/reopen; dedupe de negócio aberto por contato; `move` p/ etapa inexistente → 404; `mark_lost` sem motivo → 422 pt-BR; atividade criar/concluir/excluir; agenda_events; board endpoint; paginação (201º negócio na pág. 5), `per_page` limitado a 200, busca por título, busca com payload SQL não quebra; export → 202; board_views CRUD; bulk move; checklist/automação/cadência CRUD + validações; packs lista/traversal rejeitado; options/dashboard/audit/agent_tools | — | 61/61 OK

## Núcleo (contatos, empresas, settings, portais, relatórios — `probe/core-crud.mjs`, 100 checks)

- `[2026-09-18] …/search` | **Busca global com apóstrofo ou operador (`d'água`, `O'Brien`, `a & b`) → 500** | `SearchService#filter_messages_with_gin` monta `a <-> b` na mão e passa para `to_tsquery` → `PG::SyntaxError` | `phraseto_tsquery(?)` (mesma semântica de frase, entrada escapada) | `core/app/services/search_service.rb`, `core/spec/services/search_service_spec.rb` (+1, RED confirmado no código antigo) | RSpec 36/0
- `[2026-09-18] …/reports/*` | `since/until` não-epoch → 500 (`Date::Error`); `bot_summary` sem `type` → 500 (`nil.to_sym`) | Sem tratamento de entrada malformada | `rescue_from Date::Error` → 422; `params.require(:type)` | `core/app/controllers/api/v2/accounts/reports_controller.rb`, spec (+2) | RSpec 2/0
- `[2026-09-18] …/portals/new` | Criar portal sem `custom_domain` → 500 | `URI.parse(nil)` em `parsed_custom_domain` | guard de blank | `core/app/controllers/api/v1/accounts/portals_controller.rb`, spec (+1) | RSpec 2/0
- `[2026-09-18] …/contacts/segments, …/settings/macros/new` | API aceitava segmento/macro com nome vazio (coluna NOT NULL, mas `''` passa) | Sem `validates :name, presence` | adicionado nos dois models | `core/app/models/custom_filter.rb`, `core/app/models/macro.rb`, specs | RSpec 56/0
- `[2026-09-18] …/profile/settings` | `PUT /profile` aceitava `name: ''` | Model não valida (AgentBuilder cria convidado sem nome de propósito) | rejeita nome em branco no controller de perfil | `core/app/controllers/api/v1/profiles_controller.rb`, spec (+1) | RSpec 27/0
- `[2026-09-18] …/settings/integrations/webhook, …/settings/inboxes/new (API)` | **URL de webhook aceita `http://localhost` / IP interno (SSRF)** — admin pode fazer o servidor chamar serviços da rede interna | Comportamento herdado do Chatwoot (só valida formato http/https) | **Não corrigido**: mitigação exige resolução DNS + allow/deny list ou proxy de egress — decisão de arquitetura. ALTO no relatório | — | BLOQUEADO (decisão)
- `[2026-09-18] …/settings/automation` | API aceita `event_name` fora da lista da UI | Sem inclusion no model | Não corrigido (lista de eventos válidos vive só no front; risco de quebrar regras existentes) — BAIXO | — | BLOQUEADO (baixo)
- `[2026-09-18] …/conversations/:id` | API aceita mensagem privada com `content` vazio e sem anexo | Herdado do upstream (mensagens só-anexo são válidas) | Não corrigido — a UI já bloqueia; BAIXO | — | —
- `[2026-09-18] Núcleo — o resto` | — | — | Verificado OK: contato criar/validar e-mail/telefone/duplicado/editar/buscar/paginar/filtrar/label/merge/export/404; segmento CRUD; empresa CRUD + 404; labels (regra de título, duplicado, editar); respostas prontas CRUD + busca; times CRUD + membros; convite de agente (e-mail inválido/duplicado/role inválido → 4xx; listagem sem hash de senha); macro CRUD; atributo custom (duplicado → 4xx); automação CRUD; inbox API criar/editar/membros/agent_bot; webhook https CRUD + URL inválida; integrations/apps sem segredos; audit logs, SLA, custom roles, agent bots, assignment/capacity policies; portal categorias/artigos CRUD; campanhas; notificações (lista/read_all/unread_count); perfil sem `encrypted_password`, senha fraca → 4xx; relatórios v2 (9 endpoints), csat, sla; busca global; conversas lista/mensagens/nota privada/404 | — | 82/100 OK → após correções 90+/100 (5 itens BLOQUEADO por decisão/baixo)

## Permissões por perfil (`probe/perms.mjs`, personas operator/seller/manager/knowledge_manager/admin_b/anon)

- `[2026-09-18] …/captain/:id/config, …/captain/:id/score` | Agente abria a página e recebia 401 (`ensure_admin!` no backend); em `config` a tela ficava vazia | Rota permitia `agent`; controllers só aceitam administrador | `permissions: ['administrator']` nas duas rotas | `core/app/javascript/dashboard/routes/dashboard/captain/captain.routes.js` | Agente agora é redirecionado para o dashboard
- `[2026-09-18] …/captain/:id/scenarios` | Para agente, erro não tratado no console ("You are not authorized") | `store.dispatch('captainTools/getTools')` incondicional; `AssistantPolicy#tools?` é admin-only | Só busca tools se `isAdmin` | `.../captain/assistants/scenarios/Index.vue` | Sem erro de console para agente
- `[2026-09-18] …/crm/deals/:id` | Agente via aba "Histórico" com "Nenhuma alteração registrada" (API 401 engolida) | `CrmAuditEventPolicy#index?` admin-only; página pedia mesmo assim | Não pede e não mostra a aba para não-admin | `DealDetailsOperational.vue`, `DealDetailsAuditAccess.spec.js` (novo, 2), `DealDetailsDiscard.spec.js` (mock) | Vitest verde
- `[2026-09-18] …/portals/…/articles/new|edit` | Agente abria a edição de artigo e recebia 401 em `articles/:id` | `ArticlePolicy#show?/update?` só admin/knowledge_base_manage; rota permitia `agent` | rotas de criar/editar exigem `administrator` ou `knowledge_base_manage` | `helpcenter.routes.js` | —
- `[2026-09-18] (todas)` | Isolamento de tenant | — | Verificado: admin da conta B em 204 rotas da conta A → redirecionado ao próprio dashboard, 0 respostas 2xx de `/accounts/55/`; 20 endpoints diretos (deals, contacts, conversations, inboxes, agents, labels, pipelines, packs, reports, portals, captain, marketing, companies, custom_filters) → 404/401; POST/DELETE cross-tenant bloqueados; anônimo → 401 em todos e 204/204 rotas → `/app/login` | `probe/tenant.mjs` | 29/29 OK
- `[2026-09-18] (todas)` | Rotas sem `meta.permissions` ("autenticado, herdado" no inventário) | São containers-pai; a rota folha sempre declara permissões e `hasPermissions([])` é `false` (não abre brecha) | Sem correção — apenas esclarecido | `routeHelpers.js` | OK

## Integrações / Super Admin / Marketing / Flags

- `[2026-09-18] …/settings/integrations/{slack,linear,notion,shopify}` | 2× 404 (`/integrations/undefined.png`) a cada abertura + axe image-alt/link-name | Página passa `integration.id` undefined antes do store carregar | `<img v-if="integrationId" :alt=…>` | `settings/integrations/Integration.vue` | Sem 404 na regressão
- `[2026-09-18] /super_admin/*` (todas as 27) | Cada página disparava `GET /widget?website_token=` → 404 e depois **429** (estourava o Rack::Attack do widget) | `_javascript.html.erb` embute o SDK do widget de suporte mesmo sem token configurado | `<% if ChatwootHub.support_config[:support_website_token].present? %>` | `core/app/views/super_admin/application/_javascript.html.erb` | —
- `[2026-09-18] /super_admin/installation_configs (C13)` | **500** (`TypeError: no implicit conversion of Hash into String`) | Linha `SETUP_DONE` inserida à mão em 30/04 como JSON `{"value": true}` (o model serializa YAML dentro do jsonb); uma linha corrompida derruba a listagem inteira | **Dado local corrigido** via SQL (`UPDATE … to_jsonb(yaml)`). Código não alterado (upstream). Recomendação: rodar `SELECT name FROM installation_configs WHERE jsonb_typeof(serialized_value) <> 'string'` nas VPS antes de abrir essa tela | — | Página carrega
- `[2026-09-18] /super_admin/agent_bots/:id/edit (C20)`, `…/settings/agent-bots` | Salvava bot com nome vazio | `AgentBot` sem `validates :name` | adicionado | `core/app/models/agent_bot.rb`, spec | RSpec 67/0
- `[2026-09-18] …/marketing/*` (6 páginas) | 403 em todas as APIs para admin | Feature `marketing` desligada por padrão (`features.yml`) e o **router não aplica `meta.featureFlag`** (só a sidebar esconde) → URL direta mostra "erro de conexão" | Não alterado (comportamento upstream; página tem estado de erro). Flag ligada na conta QA para auditar as páginas → carregam OK | — | Recomendação no relatório: guard de feature no router ou mensagem "módulo não habilitado"
- `[2026-09-18] (sistema) features.yml` | **Flags nº 64 e 65 (`conversation_required_attributes`, `advanced_assignment`) não podem ser ligadas em nenhuma conta**: `ActiveModel::RangeError … out of range for Integer with limit 8 bytes` | `accounts.feature_flags` é bigint (63 bits úteis); o fork adicionou flags até 65. As rotas `settings/assignment-policy/capacity/*` dependem de `advanced_assignment` → mortas em produção | **Não corrigido** — mexer na ordem do YAML remapeia os bits de todas as contas; precisa de migration de remapeamento ou remoção de flags obsoletas. ALTO no relatório | — | BLOQUEADO (decisão)

## Deploy (18/09, 18:28–19:05 UTC)

- `[2026-09-18] (VPS Chuster + KVM4) .env de produção` | **Os releases de 17/09 perderam variáveis do `.env`**: as 3 chaves do Active Record Encryption, `SERVICE_AUTH_TOKEN`, `CORS_ORIGIN`, `LOG_LEVEL`, `EVOLUTION_EXPOSE_TOKEN_IN_FETCH` (e na KVM4 também `ORCHESTRATOR_WEBHOOK_SECRET` e 9 vars `CAPTAIN_*/LLM_*_TEST_MODEL/ORCHESTRATOR_*_LLM_MODEL`). O container `core` rodava com **0 chaves** → MFA desligado e dados criptografados desde 11/09 ilegíveis | Processo de deploy de 17/09 recriou o `.env` a partir de uma base sem essas vars | Novo release copia o `.env` atual e restaura as 8 variáveis de segurança do `.env` do release de 16/09 (chaves conferidas por sha256 com `/opt/chusterm-backups/active-record-encryption-keys.txt` em cada VPS). **Não restaurei** as 9 vars de modelo LLM da KVM4 (podem ter sido trocadas de propósito — `LLM_MODEL`/`LLM_API_KEY` mudaram) — decisão sua | `infra/release-deploy.sh` (novo, com guarda que aborta sem as 3 chaves) | Container `core` das duas VPS com 3 chaves; login inválido responde em pt-BR nos dois domínios
- `[2026-09-18] (KVM4) postgres recriado durante o deploy` | `docker compose run --rm migrate` recriou `chusterm-postgres-1` (dependência com hash de config novo) — ~30 s de banco indisponível às 19:02 UTC | `run` sem `--no-deps` | Dados intactos (mesmo volume `chusterm_postgres-data`; 275 negócios / 369 conversas / 275 elos; `ready to accept connections` sem recovery). Script passa a usar `--no-deps` | `infra/release-deploy.sh` | —
- `[2026-09-18] release `20260918T182822-c599954-checkup`` | — | — | Canário: 5 migrations aplicadas (`20260917000001`…`20260918000004`), core/sidekiq healthy, `/api` `/health` `/app/login` 200. KVM4: migrations já estavam aplicadas (`20260918000004`), 0 órfãos, 0 `installation_configs` corrompidos; core/sidekiq/orchestrator healthy. Backups em `/opt/chusterm-backups/20260918T182822-c599954-checkup/` (env, pg_dumpall, imagens `rollback-…`) nas duas VPS | — | OK
